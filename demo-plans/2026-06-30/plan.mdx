# Product Review Plan - Monday, June 30, 2026

Generated at 08:10 EDT from `demo-plans/2026-06-30`.

<Diagram
  id="review-board"
  caption="The same renderer can show a daily operating board."
  html={`
  <section class="review-board">
    <article class="review-card review-focus">
      <h3>Needs Focus</h3>
      <p>Resolve the checkout copy change before the live demo.</p>
    </article>
    <article class="review-card review-done">
      <h3>Already Handled</h3>
      <p>QA finished the regression pass and posted the findings.</p>
    </article>
    <article class="review-card review-wait">
      <h3>Waiting</h3>
      <p>Billing webhooks need production credentials from ops.</p>
    </article>
    <article class="review-card review-day">
      <h3>Calendar</h3>
      <p>Customer review at 11:00. Internal retro at 3:00.</p>
    </article>
  </section>
  `}
  css={`
  .review-board { display:grid; grid-template-columns:repeat(4,minmax(0,1fr)); gap:12px; padding:16px; background:var(--paper); }
  .review-card { border:1px solid var(--line); border-radius:10px; padding:14px; background:var(--panel); min-height:132px; }
  .review-card h3 { margin:0 0 8px; font-size:15px; }
  .review-card p { margin:0; }
  .review-focus { background:var(--warn); }
  .review-done { background:var(--ok); }
  .review-wait { background:var(--info); }
  .review-day { background:var(--panel); }
  @media (max-width: 980px) { .review-board { grid-template-columns:repeat(2,minmax(0,1fr)); } }
  @media (max-width: 640px) { .review-board { grid-template-columns:1fr; } }
  `}
/>

## Top Of Mind

1. The first screen shows the active plan, not a generic dashboard.
2. Render failures stay visible as inline warnings instead of blank gaps.
3. The sidebar lets you move between dated plans without leaving the page.

## Today

<Table
  id="today-table"
  columns={["Window", "Commitment", "Notes"]}
  rows={[
    ["09:00", "Open review prep", "Check the newest plan before the meeting."],
    ["11:00", "Customer review", "Use the operating board as the discussion frame."],
    ["15:00", "Retro", "Capture carryovers for tomorrow's plan."],
    ["End of day", "Refresh latest plan", "Keep the folder-based workflow intact."]
  ]}
/>

## Managed For You

<Checklist
  id="today-checklist"
  items={[
    { id: "one", label: "Loaded the newest plan folder automatically." },
    { id: "two", label: "Rendered diagrams, tables, and checklists server-side." },
    { id: "three", label: "Kept the UI local, fast, and dependency-free." }
  ]}
/>
